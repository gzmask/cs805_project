This is Final project for CS 805

The source code is inside 'src' folder
  typedefs, function declarations and global variable declarations are in util.h
  Global variable assignments are in main.cpp
  Function implementations are in util.cpp
  Main program is the int main() function in main.cpp
  The Web++ http server library is web++.hpp

The compile script is the compile.sh

The picture format converting script is the extract.sh

The binary executable is in 'bin' folder
